const Realisation = require("../database/models/realisations");
const path = require("path");


module.exports = function(req, res) {

    // const image = req.files

    console.log(req.files);

    // const uploadFile = path.resolve(__dirname, 'public/images_relaisations', image.name);

    // image.mv(uploadFile, function(error) {

    //     console.log(req.body);

    //     Realisation.create(req.body, function(error, post) {
    //         res.redirect('/admin')
    //     })
    // })





}